{
  "name": "acceptance",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "docker-compose build && docker-compose up",
    "codecept": "codeceptjs run-workers --suites 4 --grep @smoke --debug --plugins allure",
    "test": "docker exec acceptance npm run codecept"
  },
  "dependencies": {
    "codeceptjs": "^2.6.6",
    "codeceptjs-chai": "^2.0.1"
  },
  "devDependencies": {
    "webdriverio": "^6.3.6"
  }
}
